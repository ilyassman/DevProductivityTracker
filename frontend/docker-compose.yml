version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        - LEGACY_PEER_DEPS=true
    ports:
      - "3000:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production